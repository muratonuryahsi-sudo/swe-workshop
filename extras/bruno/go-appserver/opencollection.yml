opencollection: 1.0.0

info:
  name: go-appserver

request:
  variables:
    - name: baseUrl
      value: http://localhost:8080
    - name: ausweisUrl
      value: "{{baseUrl}}/ausweis"
    - name: ausleiheUrl
      value: "{{baseUrl}}/ausleihe"

bundled: false
extensions:
  bruno:
    ignore:
      - .git
