info:
  name: Ausweis mit nicht-vorhandener ID loeschen
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{ausweisUrl}}/:id"
  params:
    - name: id
      value: "999999"
      type: path
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
