info:
  name: Vorhandene ID 1
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/mitglied/:id"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: id
      value: "1"
      type: path

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body.id
      operator: eq
      value: "1"

settings:
  encodeUrl: true
  followRedirects: true
